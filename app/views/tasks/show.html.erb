<div>
  <%= link_to "⬅️ Home",
  tasks_path(@task) %>
</div>
<div>
  <h2><%= @task.title %></h2>
  <p><%= @task.details %></p>
  <%# <p><%= check_box(:name, :id, :checked => @task.completed?) %></p> %>
</div>
<div>
  <div>
    <%= link_to "Delete",
    task_path(@task),
    data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
  </div>
  <div>
    <%= link_to "Edit",
    edit_task_path(@task) %>
  </div>
  <div>
    <%= link_to "New task",
    new_task_path(@task) %>
  </div>
</div>
